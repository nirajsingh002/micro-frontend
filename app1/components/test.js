import * as React from 'react';

const Test = () => {
  return (
    <div
      style={{
        background: 'green',
        width: '100%',
        height: '100px',
        color: 'white',
        textAlign: 'center',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '24px',
      }}>
      Nav
    </div>
  );
};

export default Test;
